# In: scrapEngine2/.github/workflows/ci.yml
name: CI — Scrape Engine

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: munhiny/berty-cicd/.github/workflows/build-and-push.yml@main
    with:
      runner-label: arc-runner-scrape-engine
      image-name: scrape-engine
      language: node
      audit-level: high
