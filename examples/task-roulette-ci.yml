# In: task_roulette/.github/workflows/ci.yml
name: CI — Task Roulette

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: munhiny/berty-cicd/.github/workflows/build-and-push.yml@main
    with:
      runner-label: arc-runner-task-roulette
      image-name: task-roulette
      language: go,node
      node-working-directory: web
      dockerfile: deploy/Dockerfile
      audit-level: high
